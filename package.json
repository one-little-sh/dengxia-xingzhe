{
  "name": "dengxia-xingzhe",
  "private": true,
  "version": "0.1.0",
  "description": "灯下行者——AI Native 魂味探图休闲游戏（参赛作品）",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "verify": "node scripts/verify-levels.mjs",
    "backup": "node scripts/backup-levels.mjs",
    "deploy": "node scripts/deploy.mjs",
    "dev:auto": "node scripts/watch-deploy.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
